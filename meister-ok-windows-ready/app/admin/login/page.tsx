import {login} from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <section className="grid min-h-[650px] place-items-center px-5"><form action={login} className="card w-full max-w-md p-8"><div className="text-2xl font-black"><span className="text-yellow-500">MEISTER</span> OK</div><h1 className="mt-8 text-3xl font-black">Вход в управление</h1>{q.error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">Неверный email или пароль.</p>}<label className="mt-6 block"><span className="label">Email</span><input name="email" type="email" required className="field"/></label><label className="mt-4 block"><span className="label">Пароль</span><input name="password" type="password" required className="field"/></label><button className="btn btn-yellow mt-6 w-full">Войти</button></form></section>}
