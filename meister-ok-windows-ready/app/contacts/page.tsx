import {settings} from "@/lib/settings";import {Phone,MessageCircle,Mail,MapPin,Clock} from "lucide-react";
export default async function Contacts(){const s=await settings();return <section className="wrap py-20"><div className="eyebrow">Контакты</div><h1 className="display mt-4 text-7xl sm:text-9xl">НА СВЯЗИ.</h1><div className="mt-14 grid gap-5 md:grid-cols-2">{[[Phone,"Телефон",s.phone,`tel:${s.phone}`],[MessageCircle,"WhatsApp","Написать мастеру",`https://wa.me/${s.whatsapp}`],[Mail,"Email",s.email,`mailto:${s.email}`],[MapPin,"Адрес",s.address,"#"],[Clock,"Часы работы",s.hours,"#"]].map(([I,t,v,h]:any)=><a href={h} className="card p-7" key={t}><I/><p className="mt-8 text-xs font-black uppercase tracking-widest text-black/40">{t}</p><p className="mt-2 text-xl font-black">{v}</p></a>)}<div className="grid-lines min-h-64 rounded-[26px] border border-white/10 p-8"><MapPin className="text-acid"/><h2 className="mt-auto text-3xl font-black">Место для карты</h2><p className="mt-2 text-white/45">Карта появится после открытия мастерской.</p></div></div></section>}
